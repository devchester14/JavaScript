/**
 * sub string 🎆🎆
 */

const userName = "Praveen Rawat"

const subString = userName.substring(0,10)
// exclusive of end index, first param is start index last index is end index 
// can be used in usecases of viewports where you want to display partial information
console.log(subString + "...");